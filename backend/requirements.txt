# Core backend dependencies (keep this file ASCII-only: pip on Windows/GBK
# locale cannot read non-UTF8 comments, which breaks `pip install -r`).
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pillow>=10.2
httpx>=0.27
python-dotenv>=1.0
