请分析这张电商商品图片。

识别要求：
- 仅推断「可见属性」：商品类别、外观（颜色/造型/材质观感/细节）、风格、可推断的适用场景。
- 用户已补充的文字信息（可能包含材质、品牌、价格等）属于事实输入，仅放入 user_provided 字段。
- 凡是图片中看不准、对运营决策重要但你无法确认的点（如容量、尺寸、具体成分），写入 visual_uncertainty，提示用户补充，严禁猜测成事实。
- 图片中通常只有一件商品；不要把背景道具当成商品。若图片含多件明显同类商品，以居中/主体者为准。

用户补充的商品名称：{{product_name}}
用户补充说明：{{description}}

输出结构字段要求：
{{schema}}

可参考的形状示例（只是形态参考，内容必须基于本图）：
{{schema_sample}}

只输出 JSON。
